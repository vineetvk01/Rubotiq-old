import React from 'react';
import { DContainer, DRow, DCol } from '../components/Container';

const HomePage = () => {
  return (
    <DContainer fluid>
      <DRow>
        <DCol>Login</DCol>
      </DRow>
    </DContainer>
  );
};

export default HomePage;
